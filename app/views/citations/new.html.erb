<% provide(title: 'Add a citation') %>
<h1>Add new citation</h1>
<div>
  <%= form_for(@citation) do |citation| %>
    <%= citation.label :text %><br>

    <%= citation.text_area :text %><br>

    <%= citation.submit 'Add citation' %>
  <% end %>
</div>