
<% provide(:title, 'List of citations') %>

<h1>List of citations</h1>

<table>
  <% @citations.each do |citation| %>
      <tr>
        <td><%= citation.text %></td>
      </tr>
  <% end %>
</table>

<%= link_to 'Add new citation', new_citation_path %>